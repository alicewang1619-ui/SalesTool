# 设置管理

## 1. 文档元信息
| 字段 | 内容 |
| :--- | :--- |
| 所属产品 | 智能销售接待与客户洞察 Agent |
| 所属模块 | 系统配置 |
| 适用端 | Web |
| 页面路由 | `/admin/settings` |
| 页面类型 | 管理后台总览 / 设置工作台 |
| 所属阶段 | 阶段1(MVP) |
| 上游来源 | 左侧导航“配置” |
| 下游去向 | 账号权限、国家区域销售映射、产品知识库、渠道导入、提醒规则、无权限页 |

## 2. 页面概述

### 2.1 一句话定位
为管理员提供完整的系统设置入口，用于维护销售账号、角色权限、全局顶部 Banner、国家/区域销售映射、产品知识库、渠道导入参数、AI 接待边界、大模型选择和提醒规则。页面顶部必须有设置菜单栏，按菜单切换展示对应配置，避免一屏堆叠全部内容；账号导入、新增账号只出现在账号权限菜单；Banner 普通展示不出现“全局公告/查看详情”；AI 与模型支持新增模型选项并按场景绑定；配置子页必须能返回设置中心。

### 2.2 存在理由
1. 当前导航中已有“配置”，但缺少承接它的管理员总页面，用户进入后无法集中管理销售账号和权限。
2. 国家分发、销售反馈、客户池沉淀和报表统计都依赖管理员配置，必须有可视化入口保证配置完整。
3. 分散配置页需要一个总览页展示异常、最近变更和配置健康度，避免销售离职、区域缺负责人、权限误配后无人发现。

### 2.3 页面目标
| 目标维度 | 具体指标 |
| :--- | :--- |
| 可发现 | 左侧“配置”默认进入设置管理首页 |
| 可管理 | 管理员可在 3 次点击内进入销售账号、角色权限、全局 Banner、国家映射、产品知识库和大模型选择 |
| 可控 | 账号、权限和关键规则修改必须记录操作人、时间和修改前后值 |
| 可预警 | 展示停用销售仍有负责区域、国家无负责人、权限异常、渠道断连等配置风险 |

### 2.4 不做什么
- 不做完整 CRM 的组织架构、绩效和佣金管理。
- 不展示成交金额、报价金额和销售业绩金额。
- 不允许普通销售通过该页面修改自己负责区域或系统级权限。

## 3. 用户与场景

### 3.1 目标用户
| 角色 | 可见内容 | 可操作内容 |
| :--- | :--- | :--- |
| 超级管理员 | 全部设置、全部审计记录 | 新增/停用账号、分配角色、修改权限、维护所有规则 |
| 营销/运营负责人 | 线索、渠道、产品知识库、提醒规则 | 维护渠道导入、产品知识库、提醒策略；不可授予超级管理员 |
| 销售主管 | 销售账号、负责区域、未反馈规则 | 查看和申请调整销售负责区域；不可修改 RBAC |
| 销售 | 仅个人账号和反馈入口状态 | 修改个人基础信息，不可进入系统级设置 |

### 3.2 核心场景
**场景 A：新增销售账号**
- 管理员进入设置管理，点击“新增销售”。
- 填写姓名、邮箱、手机号、区域、默认语言和角色。
- 系统校验邮箱唯一性，保存后自动进入国家/区域销售映射候选负责人。

**场景 B：销售离职或暂停负责区域**
- 管理员将销售状态改为停用。
- 系统检查其负责国家/区域和未完成线索。
- 若仍有负责区域，要求先转移负责人或将相关线索进入待分配池。

**场景 C：调整权限**
- 管理员在角色权限矩阵中勾选功能权限。
- 系统提示影响范围，例如“销售将无法导出客户邮箱”。
- 保存后记录审计日志，并立即影响菜单、按钮和接口权限。

**场景 D：检查配置健康度**
- 管理员查看页面顶部风险卡片。
- 发现“6 个国家无销售负责人”“2 个渠道同步失败”。
- 点击风险项进入对应配置页处理。

## 4. 信息架构

### 4.1 页面区域
1. 顶部标题与主操作：新增销售、导入账号、查看审计。
2. 配置健康度：账号状态、权限风险、国家映射完整率、渠道同步状态。
3. 顶部 Banner 管理：上传图片、编辑文案、查看推荐尺寸/格式/大小、预览和发布；普通展示区不显示“全局公告”“查看详情”等管理或跳转字样。
4. 顶部菜单栏：总览、账号权限、Banner、线索分发、AI 与模型、审计。
5. 设置分组入口：账号与权限、销售分发、产品与 AI、渠道与提醒。
6. 销售账号管理：销售列表、角色、状态、负责区域、最近反馈。
7. 角色权限矩阵：角色与功能权限的勾选和只读说明。
8. 最近变更：展示操作人、对象、变更内容和时间。

### 4.2 设置分组
| 分组 | 子页面/能力 | 说明 |
| :--- | :--- | :--- |
| 账号与权限 | 销售账号、管理员账号、角色权限、登录安全 | 决定谁能进入系统、看到什么、执行什么 |
| 全局展示 | 顶部 Banner、公告图、跳转链接、展示状态 | 决定所有后台页面顶部统一展示内容 |
| 销售分发 | 国家区域销售映射、待分配规则、负责人转移 | 决定线索自动分发结果 |
| 产品与 AI | 产品知识库、AI 接待 Playbook、评分规则、大模型选择 | 决定 AI 引导、摘要、评分和再营销草稿生成依据 |
| 渠道与提醒 | 渠道导入、Webhook、邮箱同步、未反馈提醒 | 决定线索进入和闭环提醒 |

## 5. 界面内容

### 5.1 关键元素
| 元素 | 类型 | 默认态 | 操作后 | 禁用条件 |
| :--- | :--- | :--- | :--- | :--- |
| 新增销售 | 按钮 | 可点击 | 打开销售账号表单 | 非管理员 |
| 导入账号 | 按钮 | 可点击 | 上传账号模板 | 非管理员 |
| Banner 上传 | 文件控件 | 当前 Banner | 选择图片后生成预览 | 无 Banner 管理权限 |
| Banner 发布 | 按钮 | 草稿或当前版本 | 发布到全部后台页面 | 未上传图片或无权限 |
| Banner 下线 | 按钮 | 已启用 | 所有页面隐藏 Banner 或切换为默认公告 | 无权限 |
| 大模型选择 | 模型库/下拉框/卡片 | 当前默认模型和场景绑定 | 新增 Claude/Codex/DeepSeek 等模型，保存默认模型、邮件草稿模型、客户背景调研模型 | 模型不可用或无权限 |
| 配置健康卡 | 指标卡 | 展示风险数 | 点击进入对应配置页 | 无 |
| 销售账号表 | 表格 | 按更新时间倒序 | 可编辑角色、状态、负责区域 | 无账号管理权限 |
| 角色权限矩阵 | 表格/勾选 | 展示当前权限 | 保存后立即生效 | 非超级管理员 |
| 设置入口卡 | 导航卡 | 展示完成度和风险 | 跳转到子配置页 | 无权限时只读或隐藏 |
| 审计日志 | 时间线 | 展示最近 20 条 | 可筛选对象和操作人 | 无审计权限 |

### 5.2 字段要求
| 字段 | 说明 | 校验 |
| :--- | :--- | :--- |
| user_id | 用户唯一标识 | 系统生成，不可编辑 |
| name | 销售或管理员姓名 | 必填 |
| email | 登录邮箱和通知邮箱 | 必填、唯一、邮箱格式 |
| phone / wechat | 手机或微信通知标识 | 至少配置一种通知方式 |
| role | 超级管理员、营销/运营、销售主管、销售 | 必填 |
| account_status | active、inactive、locked | 必填 |
| owner_regions | 负责区域/国家 | 销售角色至少配置一个或允许为空进入待分配候选 |
| permission_set | 权限集合 | 只能由超级管理员修改 |
| banner_image_url | 顶部 Banner 图片地址 | 支持 PNG、JPG、WebP；推荐 1920 x 360 |
| banner_title / banner_subtitle | Banner 标题和说明 | 标题必填，说明可选 |
| banner_target_url | Banner 跳转地址 | 必须为系统内页面或允许的 HTTPS 链接 |
| banner_status | draft、active、inactive | 发布时必须写入审计日志 |

## 6. 交互流程

### 6.1 默认进入流程
```text
左侧导航点击“配置”
  -> 打开设置管理首页
  -> 查看配置健康度
  -> 点击账号权限 / 国家映射 / 产品知识库 / 渠道配置
  -> 完成配置修改
  -> 写入审计日志并回到设置管理查看状态
```

### 6.2 Banner 上传发布流程
```text
打开设置管理
  -> 进入顶部 Banner 管理
  -> 上传图片并编辑标题、说明和跳转链接
  -> 系统校验图片格式、大小、链接安全性和权限
  -> 生成预览
  -> 发布到全部后台页面
  -> 记录操作人、时间和版本
```

### 6.3 新增销售流程
```text
点击新增销售
  -> 填写姓名、邮箱、通知方式、角色和负责区域
  -> 系统校验邮箱唯一、角色权限、区域冲突
  -> 保存账号
  -> 同步到销售候选人和国家区域映射
  -> 发送初始登录通知
```

### 6.4 权限修改流程
```text
打开角色权限矩阵
  -> 修改功能权限
  -> 系统展示影响范围
  -> 二次确认
  -> 保存权限集
  -> 菜单、按钮、接口权限同步生效
  -> 写入审计日志
```

## 7. 状态与边界

| 状态 | 触发条件 | 页面表现 |
| :--- | :--- | :--- |
| 正常 | 配置完整 | 健康卡展示绿色状态 |
| 有风险 | 有国家无负责人、销售停用但仍有关联线索 | 风险卡置顶，支持跳转处理 |
| 无权限 | 当前角色无设置权限 | 隐藏敏感数据，跳转无权限页 |
| 冲突 | 两名管理员同时编辑同一配置 | 保存时提示配置已更新，要求刷新后重试 |
| 空账号 | 尚未创建销售账号 | 展示新增销售和导入模板入口 |
| 渠道异常 | Webhook/邮箱同步失败 | 在渠道卡展示最近失败时间和重试入口 |
| Banner 缺失 | 未上传或已下线 | 所有页面展示默认系统公告 Banner |
| Banner 图片过大 | 超过限制 | 阻止上传并提示压缩 |
| Banner 链接不安全 | 链接不在白名单 | 阻止发布 |

## 8. 业务规则与校验

1. 超级管理员不能删除自己，只能由另一名超级管理员停用。
2. 停用销售前必须处理其负责国家/区域和未完成线索。
3. 销售角色默认只能查看和反馈分配给自己的线索；不能导出全量客户联系方式。
4. 营销/运营可以查看报表和维护内容配置，但不能授予超级管理员权限。
5. 国家/区域映射保存后不自动重分发历史线索，需要管理员手动确认。
6. 权限修改、账号停用、负责人转移、导出配置都必须写入审计日志。
7. 账号导入必须返回成功、失败和重复账号明细。
8. 全局 Banner 发布后必须同步到所有 Web 后台页面顶部，移动端自动折行，不遮挡页面主标题和操作按钮。
9. Banner 上传只允许 PNG、JPG、WebP，建议比例为 16:3；外链跳转必须符合安全白名单。

## 9. 页面流转

```text
设置管理
  -> 账号权限
  -> 国家区域销售映射
  -> 产品知识库
  -> 渠道配置
  -> AI接待Playbook
  -> 提醒规则
  -> 无权限页
```

## 10. 数据需求

### 10.1 页面读取
| 数据 | 来源 | 用途 |
| :--- | :--- | :--- |
| 当前用户与角色 | Auth 模块 | 控制设置入口和操作权限 |
| 用户账号列表 | User / StaffAccount | 展示销售和管理员账号 |
| 角色权限集合 | Role / Permission | 渲染权限矩阵 |
| 全局 Banner 配置 | BannerConfig | 渲染所有页面顶部 Banner |
| 国家区域销售映射 | TerritoryMapping | 展示配置完整率和风险 |
| 渠道连接状态 | ChannelConfig | 展示同步状态和异常 |
| 配置审计日志 | AuditLog | 追溯变更 |

### 10.2 页面提交
| 数据 | 触发时机 | 用途 |
| :--- | :--- | :--- |
| 账号新增/编辑 | 保存账号时 | 创建或更新用户账号 |
| 角色权限变更 | 保存权限矩阵时 | 更新 RBAC |
| 负责人转移 | 转移销售负责区域时 | 更新映射并影响后续分发 |
| 渠道/提醒入口配置 | 点击子页面保存时 | 更新系统配置 |
| Banner 图片、文案和链接 | 发布 Banner 时 | 更新全部后台页面顶部展示 |

## 11. 验收标准

| 编号 | 验收点 | 通过标准 |
| :--- | :--- | :--- |
| F-01 | 设置入口存在 | 左侧“配置”点击后进入设置管理首页 |
| F-02 | 销售账号可维护 | 管理员可新增、编辑、停用销售账号 |
| F-03 | 权限可配置 | 超级管理员可维护角色权限矩阵，普通角色不可操作 |
| F-04 | 配置风险可见 | 无销售负责人、停用销售仍关联区域、渠道异常等风险在首页可见 |
| F-05 | 子配置可进入 | 可从设置管理进入国家映射、产品知识库、渠道配置、提醒规则等页面 |
| F-06 | 审计可追溯 | 账号、权限和核心配置变更记录操作人、时间和变更内容 |
| F-07 | Banner 可维护 | 管理员可上传、预览、发布和下线全局顶部 Banner |
| F-08 | Banner 全局生效 | 发布后所有 Web 后台页面顶部展示同一 Banner |
| S-01 | 权限拦截 | 无权限用户无法访问账号权限和角色配置 |
| S-02 | 敏感数据保护 | 销售账号联系方式按角色脱敏展示和导出 |

## 12. 生产化落地状态（06-30-2026）
- 真实页面路由：`/admin/settings`，作为左侧“配置”默认入口，展示统一 Banner、设置管理标题、导入账号/审计日志/新增销售按钮、四张健康度卡、顶部 Banner 管理、配置风险、设置入口、销售账号表、角色权限矩阵和最近变更。
- 返修要求：`/admin/settings` 顶部展示设置菜单栏，默认“总览”；点击不同菜单后，只展示对应设置域，不再一次性堆叠全部设置。新增“AI 与模型”菜单，展示大模型选择、产品知识库、AI 接待 Playbook 和评分规则入口。
- 返修要求补充：设置页头部不常驻“导入账号/新增账号/配置审计”按钮；账号导入和新增账号移动到“账号权限”菜单内，配置审计通过顶部菜单进入。
- Banner 要求：普通页面 `GlobalBanner` 不显示“全局公告”和“查看详情”；Banner 管理卡片展示推荐尺寸 1920×360、支持 PNG/JPG/WebP、建议不超过 2MB，并在上传后显示原始尺寸与压缩预览。
- 真实后端接口：`GET /api/settings/overview` 聚合 summary、当前全局 Banner、设置入口、销售/管理员账号、权限矩阵、配置风险和最近审计日志；仅管理员/运营可访问，销售角色返回 403。
- 真实后端接口补充：`GET /api/settings/overview` 返回 `ai_model` 配置，包含模型库、默认模型、使用场景和场景绑定；`PUT /api/settings/ai-model` 保存默认大模型、模型选项和邮件草稿/客户背景调研绑定并写入配置审计。
- 前端兼容要求：若本地开发服务尚未重启导致 `overview.ai_model` 暂缺，AI 与模型菜单必须使用默认大模型配置兜底展示，避免设置页因旧响应崩溃；保存动作仍以真实接口结果为准。
- 子页返回要求：产品知识库、国家销售映射等从设置中心进入的配置页面，左上角必须提供“返回设置中心”，并保留当前页刷新/筛选操作。
- 销售账号创建：`POST /api/settings/sales-users` 保存 name、email、password、role、data_scope、enabled，密码使用后端 hash，邮箱重复返回结构化 409，成功后写入 `settings_sales_user_created` 审计日志。
- Banner 发布：`PUT /api/settings/banner` 校验 PNG/JPG/WebP data URL、`/assets/` 或 HTTPS 图片来源，并校验跳转链接必须是 `/admin` 内部路径或 HTTPS；发布后停用旧 Banner，新增 active Banner，`GET /api/banner` 立即读取最新版本。
- 权限矩阵：`PUT /api/settings/permissions` 将角色权限保存到 `SystemSetting`，设置首页重新读取时展示最新 permissions，并写入 `settings_permissions_updated` 审计日志。
- 前端支持上传 Banner 图片到 data URL 预览、编辑 Banner 标题/说明/跳转链接、发布到全部页面、新增销售账号、保存权限矩阵和读取最近变更；页面操作全部通过真实 API，不再使用静态 mock。
- 浏览器验收：Playwright 打开 `/admin/settings` 可见设置管理、顶部 Banner 管理、设置入口、销售账号表和角色权限矩阵；新增测试销售账号成功并显示；发布 `Browser Banner Notice` 后全局 Banner 和预览同步更新；保存权限矩阵后显示成功提示并进入最近变更；控制台 warning/error 为 0。
## 13. AI 场景与邮件写手二次返修（2026-07-01）
- 账号权限菜单顶部不再展示“销售账号/角色权限”入口卡片，因为它们与下方账号表、权限矩阵重复；该菜单直接展示导入账号、新增账号、账号表和权限矩阵。
- “AI 与模型”菜单不再使用“模型库”作为主要配置区标题，改为“选择大模型”“模型场景配置”“邮件写手角色”。
- 大模型可选择、可新增；模型场景可新增、可编辑说明、可绑定模型，默认场景包含客户背景调查和邮件草稿写作。
- 邮件写手角色可新增、编辑风格和技能；默认角色包含 Doraemon、Mario、Pikachu、Totoro、Baymax、Nemo。角色配置会同步给再营销草稿详情页。
- 再营销草稿页选择邮件写手后，保存和重新生成草稿都必须持久化 `writer_role_key`，并把角色风格和技能写入上下文快照。
## GOAL 生产化路由同步
- 生产路由：`/admin/settings`。
- 生产实现以 React/Vite 路由和 FastAPI 真实 API 为准，HTML 文件仅作为视觉、布局和组件蓝图，不作为可执行生产代码。
## 2026-07-01 二次统一返修
- 新增“邮件接口”菜单/配置区：管理员和运营配置全局发信邮箱、SMTP 或邮件服务参数、发件人名称、TLS、测试连接、测试发送、启用状态；销售个人邮箱仍在“我的”配置。
- “线索分发”内客户来源字典、渠道配置、提醒规则都必须是可编辑配置，不再只展示说明卡片；每块支持新增、编辑、启停、保存和审计。
- 客户来源字典字段包含来源名称、来源类型/分组、系统键、导入校验规则、启用状态；保存后驱动线索池来源筛选和导入校验。
- 全局 Banner 发布后必须刷新当前预览和所有后台页顶部 Banner；全局展示组件不得使用写死旧值或旧缓存。

## 2026-07-01 三次统一返修设计补充
- 设置页邮件接口区使用表单标签 + 输入框的垂直节奏，每个字段上方显示标题，占位符只做示例，不承担说明职责。
- AI 与模型采用三段折叠：①场景模型绑定（先选场景，再选模型）②大模型连接配置（选择模型摘要后展开详情/新增弹窗）③邮件写手角色（选择角色摘要后弹窗编辑）。
- 大模型新增弹窗采用从上到下表单：基础信息、API 连接、能力说明、启用状态；API Key 输入后仅显示“已配置/未配置”，避免明文长期展示。
- 邮件写手角色采用角色卡片下拉/折叠摘要，点击“编辑角色”打开弹窗，字段顺序为英文标识、中文名、风格、技能、适用场景、状态。
- 登录页采用独立居中卡片 + 蓝紫渐变背景，不挂载后台菜单；错误提示、快捷测试账号和登录按钮在同一视觉焦点内。
- AppShell 左侧菜单固定 100vh，主内容独立滚动；详情页页头动作区固定在全局 Banner 下方首屏，返回按钮靠左或动作区首位。
- 客户来源字典采用类型下拉/折叠，不在首屏渲染全部来源行；当前类型显示来源项编辑表单和保存按钮。
## 2026-07-01 三次统一返修开发口径
- 邮件接口配置表单必须显示字段标题：发信邮箱、发件人名称、SMTP Host、SMTP Port、SMTP 用户名、SMTP 密码/应用专用密码、测试收件人邮箱；TLS 和全局主邮箱开关必须有清晰标签。
- AI 与模型配置必须以“场景优先”为主流程：先选择或新增模型使用场景，再为该场景绑定大模型；场景默认包含客户背景调查、邮件草稿写作，并允许后续新增报价跟进、线索摘要等场景。
- 大模型库必须支持新增真实可启用配置：模型 Key、模型名称、供应商、API Base URL、Endpoint Path、鉴权方式、API Key/Token（前端输入，后端只回显已配置状态）、能力说明、适用场景和启用状态。
- AI 与模型页面不得一次性铺满模型库和邮件写手；模型库、场景绑定、邮件写手均采用下拉/折叠式摘要展示，点击后再展开或打开弹窗编辑。
- 邮件写手角色配置必须通过角色摘要卡/下拉选择进入编辑弹窗；弹窗自上而下编辑英文标识、中文名、风格、技能、适用场景、启用状态，不能使用横向表格行内编辑。
- 登录页必须是独立视觉页面，未登录状态不展示后台左侧菜单、后台 Banner 或后台业务导航；登录成功后才进入后台 AppShell。
- 后台左侧菜单必须固定在视口，不跟随主内容滚动；主内容区域独立滚动，所有详情页顶部提供清晰返回按钮（返回列表/返回上一页）。
- 客户来源字典必须改成下拉/折叠展示：默认展示来源类型摘要和数量，点击某一类型后仅展示该类型下来源项，可新增、编辑、启停和保存，避免所有来源一次性铺开。

## 2026-07-01 三次统一返修实现回写
- 已实现邮件接口字段标题：发信邮箱、发件人名称、SMTP Host、SMTP Port、SMTP 用户名、SMTP 密码/应用专用密码、测试收件人邮箱均有可见标签。
- 已实现 AI 场景优先配置：设置页先选择模型场景，再为场景绑定模型；新增场景使用弹窗；模型库通过下拉选择和弹窗编辑，不再全量表格铺开。
- 已实现大模型 API 配置字段：模型 Key、名称、供应商、API Base URL、Endpoint Path、鉴权方式、API Key 配置状态、启用状态；后端保存 API Key 配置状态但不在响应中回显明文 API Key。
- 已实现邮件写手下拉/弹窗：默认写手和当前编辑写手通过下拉选择，点击编辑打开纵向表单配置英文角色、中文名、风格、技能、适用场景和状态。
- 已实现独立登录页：未登录状态只展示登录卡片和产品说明，不展示后台左侧菜单或全局后台 Banner。
- 已实现后台布局固定：左侧菜单固定 100vh，主内容区域独立滚动；现有线索详情、客户详情、再营销详情、产品知识库和国家销售映射均保留返回按钮。
- 已实现客户来源字典下拉维护：先选择来源类型，再只展示该类型下来源项，支持当前类型新增、编辑、启停和保存。
- 验证：frontend 执行 `npm.cmd run build` 通过；backend 设置专项执行 `py -m pytest .\tests -q -k "settings_ai_model_library_bindings_can_be_saved_and_used_by_nurture_regeneration or settings_ai_model_scenarios_and_email_writers_can_be_saved or settings"` 结果 15 passed；backend 全量执行 `py -m pytest .\tests -q` 结果 103 passed。

## 2026-07-02 全局流程返修补充
- 账号权限：编辑账号弹窗必须提供“删除账号”危险按钮；删除前二次确认。当前登录用户、最后一个管理员、仍有负责客户/国家映射的账号不能直接删除，应提示先转移或停用。
- AI 与模型：模型场景、大模型连接配置、邮件写手角色都必须支持删除。被当前场景绑定的大模型、默认写手、最后一个启用写手、默认场景不允许静默删除，需提示先切换绑定或保留至少一个可用项。
- Banner：设置页预览和全局展示的字体颜色保持一致，标题/正文均为白色，页面基础标题样式不得覆盖 Banner 字色。

## 2026-07-02 二次返修补充
- 工作台首页只保留筛选、可跳转指标卡和继续处理快捷入口；不得再展示底部 AI 摘要、分发与反馈或客户/线索明细表。
- 待分配确认时管理员/运营只选择销售并点击确认；分配后系统态显示“已分配 / 待销售反馈”，该状态不属于销售反馈状态选项，客户需立即从待分配列表移除。
- 客户来源字典必须支持新增、编辑、启用/停用和删除式操作；已被历史引用的来源不硬删除，采用停用方式从筛选和导入校验中移除。
- 产品知识库需要支持产品、竞品、市场以及自定义知识库板块；自定义板块可新增、重命名和删除，仍有知识条目的板块不得静默删除。
- 群发邮件放在“再营销 / 群发邮件”，仅管理员和运营可见；群发目的包含“开发信、活动推广、自定义类型”，选择后自动带出主题模板、正文模板和生成 Prompt，并允许选择邮件写手和上传 PDF/Word/Excel 参考附件。
- 群发邮件第一版只创建草稿/模板/预览和审计记录，不绕过人工确认直接发送；真正发送依赖邮箱接口配置成功后的人工确认流程。
- 再营销页面内蓝色说明框改为悬浮提示/说明按钮，减少页面铺满说明文本；单客户附件支持 PDF、Word、Excel。
## 2026-07-02 邮件写手角色 UI 调整
- “默认邮件写手”和“角色下拉菜单”只显示英文角色名，不再在名字后拼接风格、角色描述或标签。
- 角色目标、能力与技能方向、相关技能、背景定义和角色标签通过鼠标悬停/点击角色名的提示框展示。
- 邮件写手区域顶部操作只保留“新增写手”“保存写手配置”；当前选中角色的“编辑”“删除”放到右侧详情卡片底部。

## 2026-07-02 大模型连接配置 UI 调整
- 大模型连接配置顶部操作只保留“添加大模型”“保存模型库”。
- 当前选中模型的“编辑”“删除”放到右侧模型详情卡片底部，不再与顶部新增/保存动作混在一起。
- 删除模型继续保留二次确认；若模型已被场景绑定，删除后按现有逻辑回退到其他可用模型。

## 2026-07-02 信息型蓝框清理补充
- 设置页总览、Banner、客户来源、邮件接口、AI 场景等说明不再使用蓝色信息框；改为普通辅助文案、字段说明或悬浮提示。
- 设置页仍保留保存成功、操作失败、删除确认等状态反馈，不把普通说明做成 Alert。

## 2026-07-03 营销技能菜单调整
- 左侧主导航新增“营销技能”一级入口；邮件写手从“AI 与模型”迁移到该独立入口中配置，进入后不展示“配置中心”标题、说明、顶部配置 Tab、总览卡片或其他配置内容。
- “AI 与模型”只保留场景模型绑定、大模型连接配置、产品/竞品/市场知识库入口，不再展示邮件写手区域。
- “营销技能”默认展示“邮件写手配置”：顶部只保留标题、说明、“新增写手”和“保存写手配置”；主体左侧展示默认写手、当前编辑写手和写手列表，右侧展示当前写手详情卡。
- 页面文案必须强调“每个邮件写手是一套营销技能 / 后台 Prompt 配置”，并说明会进入再营销写邮件的大模型生成上下文。
- 左侧主导航“营销技能”放在“再营销”之后、“报表”之前；点击后进入邮件写手配置独立页，左侧菜单高亮“营销技能”；`section=marketing` 必须映射到该独立页，不得回到总览或配置中心壳子。
