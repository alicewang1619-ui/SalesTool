# 2026-07-01 统一返修 TDD

## 覆盖目标

覆盖用户集中反馈的设置页、Banner、Excel 导入、国家销售自动分配、再营销邮件发送、“我的”模块、客户详情、时间维度和权限导航；追加覆盖设置页顶部菜单分组、大模型模型库/场景绑定、Banner 无噪声文案、Banner 上传尺寸提示和配置子页返回路径。

## P0 集成契约

| 编号 | 场景 | Given | When | Then |
|---|---|---|---|---|
| I-REFIT-01 | 导入模板下载 | 管理员登录 | GET `/api/import-template` | 返回 CSV 模板，包含 customer_name、email、organization、country、customer_type、product、source_category、source_label、raw_inquiry 字段 |
| I-REFIT-02 | 导入自动分配 | Peru 已映射给 Maria | 上传 Peru 客户 XLSX | 导入任务成功，返回 auto_assigned_rows >= 1，线索 owner_id 为 Maria |
| I-REFIT-03 | 销售再营销范围 | Maria 登录 | GET `/api/nurture-tasks` | 200，仅返回本人分配或负责国家客户 |
| I-REFIT-04 | 销售非本人任务越权 | Maria 登录 | GET 非本人范围 `/api/nurture-tasks/:id` | 403，写 permission_denied 审计 |
| I-REFIT-05 | 我的资料 | 任意角色登录 | GET/PUT `/api/me/profile` | 只能读取/修改本人姓名与邮箱配置 |
| I-REFIT-06 | 修改密码 | 任意角色登录 | PUT `/api/me/password` | 旧密码正确时保存并写审计，错误时 400 |
| I-REFIT-07 | 周期报表时间说明 | 管理员选择季度 | GET `/api/reports/period?period=quarter` | 返回 period_label、period_granularity、query_window.start_at/end_at |
| I-REFIT-08 | 客户详情基础信息 | 管理员打开客户详情 | GET `/api/customers/:id` | 返回 email、organization、demand_summary、first_inquiry_at、source_summary 和 signals |

## P0 组件 / E2E

| 编号 | 页面 | 验收点 |
|---|---|---|
| C-REFIT-01 | AppShell | 销售登录后不展示设置、报表、待分配等无权限入口；展示再营销和我的。 |
| C-REFIT-02 | GlobalBanner | 普通页面只展示 Banner 图片、标题和正文，无“全局公告”“查看详情”“管理 Banner”按钮；宽度适配主内容。 |
| C-REFIT-03 | LeadImportPage | 选择 Excel 后立刻展示文件名、模板说明、上传进度和自动分配结果。 |
| C-REFIT-04 | NurtureTaskDetailPage | 显示发件人、收件人、邮件主题、正文、参考素材和发送状态。 |
| C-REFIT-05 | CustomerDetailPage | 首屏展示基础信息、询盘需求、背景调查、态势信号。 |
| C-REFIT-06 | SettingsPage | 顶部菜单切换设置域；账号动作只在账号权限菜单；AI 与模型菜单展示模型库、添加模型，并保存邮件草稿/客户背景调研模型绑定。 |
| C-REFIT-06B | SettingsPage / NurtureTaskDetailPage | AI 与模型菜单改为选择大模型、模型场景配置、邮件写手角色；场景和写手均可新增，草稿详情可选择写手角色并写入上下文。 |
| C-REFIT-07 | ProductKnowledgePage | 从设置中心进入后，页面头部展示返回设置中心按钮。 |
| E-REFIT-01 | 工作台 → 线索池 | 点击今日询盘跳转 `/admin/leads?time_scope=today` 并展示筛选摘要。 |
| E-REFIT-02 | 导入 → 待分配 | 导入无映射国家客户后，结果显示待人工确认并可跳转待分配列表。 |
| E-REFIT-03 | 销售再营销 | 销售登录进入再营销，只看到本人范围客户，确认发送前必须看到发件人/收件人。 |

## P0 安全

- 销售不能通过直接 URL 访问设置、报表、待分配接口。
- 销售不能读取或发送非本人客户范围内再营销任务。
- 未配置个人邮箱且无可用主邮箱时，再营销确认发送返回结构化错误，不写发送成功审计。
- 前端隐藏入口不能作为唯一权限控制；后端必须保留鉴权。

## 2026-07-01 实现验收回写

| 门禁 | 命令 | 结果 |
|---|---|---|
| 后端返修专项 | `cd backend && py -m pytest .\tests -q -k "REFIT or import_template or auto_assigns or time_scope or basic_info_demand or me_profile or report_period_aggregates or scoped_nurture or customer_signals"` | 9 passed，保留既有 deprecation warnings |
| 后端全量 | `cd backend && py -m pytest .\tests -q` | 96 passed，保留既有 deprecation warnings |
| 前端构建 | `cd frontend && npm.cmd run build` | 通过，保留既有 Vite chunk size warning |

补充验收点：
- 配置页账号管理不再只是展示启用状态，已具备新增、编辑和软启停能力。
- 再营销草稿的邮件主题可保存，确认发送时随正文一起提交。
- 国家销售映射页保留 `pending_count` 展示待处理线索，但不把待处理数量误判为映射配置风险。

## 2026-07-01 三次统一返修 TDD
| 编号 | 优先级 | 场景 | 验收标准 |
|---|---|---|---|
| SETTINGS-MAIL-LABEL-01 | P0 | 管理员打开邮件接口配置 | 每个输入项都有可见字段标题，不能只靠 placeholder 说明 |
| SETTINGS-AI-SCENE-01 | P0 | 管理员配置 AI 场景 | 页面先选择场景，再为该场景选择模型；可新增场景并保存后从接口返回 |
| SETTINGS-AI-MODEL-API-01 | P0 | 管理员新增大模型 | 表单包含模型 Key、供应商、API Base URL、Endpoint、鉴权方式、API Key 配置状态、启用状态；后端保存后不明文回显 API Key |
| SETTINGS-AI-COLLAPSE-01 | P1 | 打开 AI 与模型页 | 模型库和邮件写手默认不全量铺开，通过下拉/折叠/弹窗查看详情 |
| SETTINGS-WRITER-MODAL-01 | P0 | 编辑邮件写手角色 | 点击角色后打开弹窗，按纵向表单编辑风格、技能、适用场景并保存 |
| LOGIN-SHELL-01 | P0 | 未登录访问登录页 | 不出现后台左侧菜单、后台 Banner 和后台业务导航 |
| APP-SHELL-FIXED-01 | P1 | 后台页面滚动 | 左侧菜单固定在视口，主内容滚动不带动菜单滚动 |
| DETAIL-BACK-01 | P1 | 打开再营销/客户/线索详情 | 顶部页头提供返回列表或返回上一页按钮 |
| SOURCE-DICT-COLLAPSE-01 | P0 | 配置客户来源字典 | 默认仅显示来源类型摘要，选择/展开类型后才能编辑该组来源并保存 |

## 2026-07-01 三次统一返修实现回写
- 已实现邮件接口字段标题：发信邮箱、发件人名称、SMTP Host、SMTP Port、SMTP 用户名、SMTP 密码/应用专用密码、测试收件人邮箱均有可见标签。
- 已实现 AI 场景优先配置：设置页先选择模型场景，再为场景绑定模型；新增场景使用弹窗；模型库通过下拉选择和弹窗编辑，不再全量表格铺开。
- 已实现大模型 API 配置字段：模型 Key、名称、供应商、API Base URL、Endpoint Path、鉴权方式、API Key 配置状态、启用状态；后端保存 API Key 配置状态但不在响应中回显明文 API Key。
- 已实现邮件写手下拉/弹窗：默认写手和当前编辑写手通过下拉选择，点击编辑打开纵向表单配置英文角色、中文名、风格、技能、适用场景和状态。
- 已实现独立登录页：未登录状态只展示登录卡片和产品说明，不展示后台左侧菜单或全局后台 Banner。
- 已实现后台布局固定：左侧菜单固定 100vh，主内容区域独立滚动；现有线索详情、客户详情、再营销详情、产品知识库和国家销售映射均保留返回按钮。
- 已实现客户来源字典下拉维护：先选择来源类型，再只展示该类型下来源项，支持当前类型新增、编辑、启停和保存。
- 验证：frontend 执行 `npm.cmd run build` 通过；backend 设置专项执行 `py -m pytest .\tests -q -k "settings_ai_model_library_bindings_can_be_saved_and_used_by_nurture_regeneration or settings_ai_model_scenarios_and_email_writers_can_be_saved or settings"` 结果 15 passed；backend 全量执行 `py -m pytest .\tests -q` 结果 103 passed。
